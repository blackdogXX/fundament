version: '3.8'
services:
  fundament:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_EMAIL=${ADMIN_EMAIL:-}
    restart: unless-stopped
